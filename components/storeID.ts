// import { create } from 'zustand'

// type CartStore = {
//     cart: number,
//     add: (Array<number>) => void:any,
//     remove: () => void,
//     removeAll: () => void
// }

// export const useCartStore = create<CartStore>((set) => ({
//     cart: 0,
//     add: (id: Array<number>) => set((state) => ({ cart: id })),
//     remove: () => set((state) => ({ cart: state.cart - 1 })),
//     removeAll: () => set({ cart: 0 }),
// }));
